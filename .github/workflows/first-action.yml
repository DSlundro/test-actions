name: First Workflow
on: workflow_dispatch
jobs:
  first_job: 
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello World!"
          echo "2nd"
      - name: Print goodbye
        run: echo "Done - bye!"
